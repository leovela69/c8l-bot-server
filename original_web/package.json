{
  "name": "c8l-agency",
  "version": "1.0.0",
  "description": "C8L Agency Mobile App",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step required for static files'",
    "copy": "npx cap copy",
    "sync": "npx cap sync",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
